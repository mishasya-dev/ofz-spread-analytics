# Результаты ревизии проекта OFZ Spread Analytics

**Дата последнего обновления:** 2026-03-15
**Ветка:** feature/g-spread-yearyields-method
**Последний коммит:** `f2c4303` - fix: handle weekends in zcyc tests

---

## ✅ МИГРАЦИЯ ЗАВЕРШЕНА

Все файлы успешно мигрированы с `core/database.py` на новую архитектуру `core/db/`.

### Мигрированные файлы:

| # | Файл | Статус |
|---|------|--------|
| 1 | `services/data_loader.py` | ✅ Мигрирован |
| 2 | `components/db_panel.py` | ✅ Мигрирован |
| 3 | `components/bond_manager.py` | ✅ Мигрирован |
| 4 | `core/cointegration_service.py` | ✅ Мигрирован |
| 5 | `core/session.py` | ✅ Мигрирован |
| 6 | `app.py` | ✅ Мигрирован |

---

## 🚀 Последние оптимизации (13.03.2026)

### ZCYC Loading Optimization (5x speedup)

**Проблема:** ZCYC загружался отдельно для каждой облигации = N × D запросов к MOEX

**Решение:** MOEX API возвращает ВСЕ 32 облигации за один запрос!
```
Было:  N облигаций × D дней запросов
Стало: D дней запросов (все облигации вместе)
```

**Изменения в `api/moex_zcyc.py`:**
1. Проверяем кэш БЕЗ фильтрации по ISIN (получаем все даты)
2. Загружаем ВСЕ облигации с MOEX (не фильтруем при загрузке)
3. Сохраняем ВСЕ облигации в БД (`zcyc_cache`)
4. Фильтруем по ISIN только при возврате результата

**Результат:**
| Метрика | До | После |
|---------|-----|-------|
| Запросов для 5 облигаций, 250 дней | 1250 | 250 |
| Сокращение | - | **5x** |

### Bug Fixes

1. **StreamlitDuplicateElementId** - добавлены уникальные ключи в `st.plotly_chart()` для G-spread графиков
2. **Warning при выборе одинаковых облигаций** - добавлено предупреждение когда пользователь выбирает одну облигацию в обоих dropdown

---

## 1. Структура проекта

```
ofz-spread-analytics/
├── api/                    # API клиенты MOEX
│   ├── __init__.py
│   ├── moex_bonds.py       # Получение списка облигаций ✅ используется
│   ├── moex_candles.py     # Свечи ✅ используется
│   ├── moex_client.py      # ✅ Единый клиент с rate limiting
│   ├── moex_history.py     # Исторические данные ✅ используется
│   ├── moex_trading.py     # Статус торгов ⚠️ используется только в modes/
│   ├── moex_zcyc.py        # ZCYC (G-spread) ✅ активно используется (оптимизировано!)
│   └── README.md           # Документация API
│
├── components/             # UI компоненты Streamlit
│   ├── __init__.py
│   ├── bond_manager.py     # Управление избранными облигациями ✅
│   ├── charts.py           # Графики Plotly ✅ активно используется
│   ├── db_panel.py         # Панель БД ✅
│   ├── header.py           # Заголовок ✅
│   ├── metrics.py          # Метрики ✅
│   ├── sidebar.py          # Боковая панель ✅
│   ├── signals.py          # Карточки сигналов ✅
│   └── styles.py           # CSS стили ✅
│
├── core/                   # Бизнес-логика
│   ├── db/                 # Репозитории БД
│   │   ├── __init__.py
│   │   ├── bonds_repo.py   # ✅ используется
│   │   ├── connection.py   # ✅ активно используется
│   │   ├── facade.py       # ✅ используется
│   │   ├── g_spread_repo.py# ✅ активно используется (zcyc_cache + empty_dates)
│   │   ├── spreads_repo.py # ✅ используется
│   │   └── ytm_repo.py     # ✅ используется
│   │
│   ├── __init__.py
│   ├── backtest.py         # ⚠️ НЕ используется в app.py
│   ├── cointegration.py    # ✅ используется
│   ├── cointegration_service.py # ✅ используется
│   ├── exceptions.py       # ❌ DEAD CODE - не импортируется
│   ├── ofz_cache.py        # ✅ используется
│   ├── session.py          # ⚠️ НЕ используется в app.py
│   ├── signals.py          # ⚠️ НЕ используется напрямую (есть дубликат)
│   ├── spread.py           # ⚠️ НЕ используется в app.py
│   ├── types.py            # ✅ используется (TypedDict)
│   └── ytm_calculator.py   # ✅ используется
│
├── export/                 # Экспорт сигналов
│   ├── __init__.py
│   ├── formatters.py       # ❌ DEAD CODE - не используется
│   └── signal_sender.py    # ❌ DEAD CODE - не используется
│
├── models/                 # Модели данных
│   ├── __init__.py
│   └── bond.py             # ✅ используется
│
├── modes/                  # Режимы работы (устаревшие?)
│   ├── __init__.py
│   ├── base.py             # ⚠️ НЕ используется в app.py
│   └── intraday.py         # ⚠️ НЕ используется в app.py
│
├── services/               # Сервисы
│   ├── __init__.py
│   ├── candle_processor_ytm_for_bonds.py # ✅ активно используется
│   ├── candle_service.py   # ✅ используется
│   ├── data_loader.py      # ✅ используется
│   ├── g_spread_calculator.py # ✅ активно используется
│   ├── spread_calculator.py   # ✅ используется
│   └── state_manager.py    # ✅ используется (URL sync)
│
├── tests/                  # Тесты
│   ├── test_zcyc_optimization.py # ✅ тесты оптимизации ZCYC
│   ├── test_g_spread_calculator.py # ✅ NEW - тесты G-spread
│   ├── test_metrics_signals.py    # ✅ NEW - тесты metrics/signals
│   └── ...
│
├── utils/                  # Утилиты
│   ├── __init__.py
│   └── bond_utils.py       # ✅ используется
│
├── app.py                  # ГЛАВНЫЙ ФАЙЛ ✅
├── config.py               # Конфигурация ✅
├── SESSION_CONTEXT.md      # ✅ Контекст сессии
└── ... (остальные файлы)
```

---

## 2. DEAD CODE (Неиспользуемый код)

### 2.1. Полностью неиспользуемые модули

| Файл | Причина |
|------|---------|
| `core/exceptions.py` | Не импортируется нигде. Определены классы исключений, но они не используются |
| `export/formatters.py` | Не импортируется в app.py. SignalFormatter, JSONFormatter и т.д. |
| `export/signal_sender.py` | Не импортируется в app.py. SignalSender, WebhookSender и т.д. |

### 2.2. Модули, не используемые в основном приложении (app.py)

| Файл | Символ | Примечание |
|------|--------|------------|
| `core/backtest.py` | `Backtester`, `BacktestResult` | Экспортируется, но не используется в UI |
| `core/session.py` | `SessionManager` | Не импортируется в app.py |
| `core/spread.py` | `SpreadCalculator` | Есть дубликат в services/ |
| `core/signals.py` | `SignalGenerator` | Есть дубликат в services/ |
| `modes/base.py` | `DailyMode` | Устаревший режим работы |
| `modes/intraday.py` | `IntradayMode` | Устаревший режим работы |

### 2.3. Deprecated функции (помечены как устаревшие)

| Файл | Функция | Замена |
|------|---------|--------|
| `api/moex_zcyc.py` | `get_current_clcyield()` | `get_zcyc_data_for_date()` |
| `api/moex_candles.py` | `fetch_candles_with_ytm()` | `BondYTMProcessor` |
| `api/moex_candles.py` | `get_hourly_candles_with_ytm()` | `get_raw_candles()` |
| `api/moex_candles.py` | `get_today_hourly_ytm()` | `get_raw_candles()` |
| `services/g_spread_calculator.py` | `nelson_siegel()` | `get_zcyc_data_for_date()` |
| `services/g_spread_calculator.py` | `calculate_g_spread()` | `get_zcyc_history_parallel()` |
| `services/g_spread_calculator.py` | `GSpreadCalculator` | MOEX API |

---

## 3. Дублирование кода

### 3.1. Signal Generator
- **`core/signals.py`**: `SignalGenerator`, `TradingSignal`
- **`services/spread_calculator.py`**: `generate_signal()`
- **`components/signals.py`**: `generate_signal()` (другая реализация!)

### 3.2. Spread Calculator
- **`core/spread.py`**: `SpreadCalculator`
- **`services/spread_calculator.py`**: `calculate_spread_stats()`, `calculate_rolling_stats()`

### 3.3. Bond utilities
- **`utils/bond_utils.py`**: `BondItem`, `get_years_to_maturity()`
- **`components/sidebar.py`**: те же функции

### 3.4. calculate_spread_stats
- **`components/metrics.py`**: `calculate_spread_stats()`
- **`components/signals.py`**: `calculate_spread_stats()` (дубликат!)

---

## 4. Проблемы архитектуры

### 4.1. ✅ РЕШЕНО: Единая архитектура БД
- **Ранее:** Два уровня БД (`core/database.py` и `core/db/`)
- **Сейчас:** Используется только `core/db/` с репозиториями

### 4.2. Экспорт в `__init__.py` не соответствует использованию

**`/__init__.py` экспортирует:**
```python
"TradingChecker", "TradingStatus",  # api - используется только в modes/
"HistoryFetcher", "CandleFetcher",   # api - используется
"YTMCalculator", "SpreadCalculator", # core - SpreadCalculator НЕ используется в app.py
"SignalGenerator", "TradingSignal",  # core - НЕ используется в app.py
"Backtester",                        # core - НЕ используется
"SignalSender", ...                  # export - НЕ используется
"DailyMode", "IntradayMode"          # modes - НЕ используется в app.py
```

---

## 5. Рекомендации

### 5.1. Удалить (полностью)
1. `core/exceptions.py` - не используется
2. `export/` директория - функционал не реализован в UI
3. Закомментированный DEPRECATED код в `api/moex_zcyc.py`
4. Закомментированный DEPRECATED код в `services/g_spread_calculator.py`

### 5.2. Рефакторинг
1. **Удалить дубликаты**: оставить `services/spread_calculator.py`, удалить `core/spread.py`
2. **Удалить дубликаты**: оставить `components/signals.py`, удалить `core/signals.py`
3. **Удалить modes/**: функционал интегрирован в `app.py`
4. **Удалить дубликат**: оставить `components/metrics.py`, убрать `calculate_spread_stats` из `components/signals.py`

### 5.3. Обновить экспорт
Очистить `__init__.py` от неиспользуемых экспортов:
```python
# Убрать:
- SignalSender, JSONFormatter, TelegramFormatter, WebhookFormatter
- DailyMode, IntradayMode
- Backtester, BacktestResult
- SpreadCalculator (core)
- SignalGenerator (core)
```

---

## 6. Статистика

| Категория | Количество |
|-----------|------------|
| Всего .py файлов | ~55 |
| Файлов в tests/ | 30 |
| DEAD CODE модулей | 3 |
| Deprecated функций | 7 |
| Дублирующихся компонентов | 4 |

---

## 7. Выполненные задачи

### Миграция и архитектура
- [x] Миграция на `core/db/` архитектуру
- [x] Unified action logging system
- [x] Context managers для БД

### G-Spread
- [x] Implement `get_zcyc_data_for_date()` - exact G-spread from MOEX
- [x] Implement `get_zcyc_history_parallel()` - parallel loading with 5 workers
- [x] Add `zcyc_cache` table for caching ZCYC data
- [x] Add `zcyc_empty_dates` table for holidays
- [x] **Optimize ZCYC loading - load ALL bonds at once (5x speedup)**

### Bug Fixes
- [x] Fix StreamlitDuplicateElementId for G-spread charts
- [x] Add warning when same bond selected in both dropdowns
- [x] Fix sliders/radio not changing values (state_manager.py)
- [x] Fix favorites conflicts between tabs (app.py)
- [x] Add refresh button for favorites sync
- [x] Remove browser-storage dependency
- [x] Implement URL-based state persistence

### Tests
- [x] Create `test_zcyc_optimization.py`
- [x] Create `test_g_spread_calculator.py` - NEW!
- [x] Create `test_metrics_signals.py` - NEW!
- [x] Remove obsolete browser-storage tests
- [x] Fix zcyc tests for weekends (find last trading day)

### Documentation
- [x] Update README with new architecture
- [x] Update SESSION_CONTEXT.md
- [x] Update результаты_ревизии.md

---

## 8. Тестовое покрытие

### Покрытие по модулям (оценка)

| Статус | Модули |
|--------|--------|
| **✅ Хорошее (>70%)** | api/moex_client.py, api/moex_bonds.py, core/ytm_calculator.py, core/cointegration.py, core/cointegration_service.py, core/db/connection.py, core/db/bonds_repo.py, core/db/ytm_repo.py, services/candle_service.py, services/candle_processor_ytm_for_bonds.py, services/spread_calculator.py, services/state_manager.py, services/g_spread_calculator.py, components/sidebar.py, components/charts.py, components/bond_manager.py, models/bond.py |
| **⚠️ Частичное (30-70%)** | api/moex_candles.py, api/moex_history.py, api/moex_zcyc.py, core/ofz_cache.py, core/spread.py, core/db/g_spread_repo.py, core/db/facade.py, services/data_loader.py, components/metrics.py, components/signals.py |
| **❌ Нет тестов** | api/moex_trading.py, core/session.py, core/signals.py, core/backtest.py, core/exceptions.py, utils/bond_utils.py, modes/base.py, modes/intraday.py, export/formatters.py, export/signal_sender.py |

### Общая статистика
- **Хорошее покрытие:** 17 модулей (44%)
- **Частичное покрытие:** 10 модулей (26%)
- **Без тестов:** 12 модулей (31%)

---

## 9. Потенциальные будущие задачи

### Высокий приоритет
- [ ] Add tests for core/signals.py (SignalGenerator)
- [ ] Add tests for core/backtest.py (Backtester)
- [ ] Add tests for api/moex_trading.py

### Средний приоритет
- [ ] Add daily snapshot job for ZCYC data
- [ ] Implement G-spread alerts/notifications
- [ ] Create API documentation with OpenAPI
- [ ] Merge feature branch to master

### Низкий приоритет (cleanup)
- [ ] Cleanup DEAD CODE modules (export/, core/exceptions.py)
- [ ] Remove duplicate code (core/signals.py, core/spread.py)
- [ ] Remove modes/ directory
- [ ] Update __init__.py exports

---

*Ревизия обновлена: 2026-03-15 UTC*
